<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pantryItem.name %></title>
  </head>
  <body>
        <%- include('../partials/_navbar.ejs') %>
        <h1><%= pantryItem.name %></h1>
        <div class="show-actions">
            <a
                href="/users/<%= user._id %>/foods/<%= pantryItem._id %>/edit"
            >
                Edit Food
            </a>
            <form
                action="/users/<%= user._id %>/foods/<%= pantryItem._id %>?_method=DELETE"
                method="POST"
            >
                <button type="submit">Delete Food</button>
            </form>
        </div>
  </body>
</html>